# Medha mobile (Expo) — copy to .env
#
# Phone on Expo Go must reach your PC by LAN IP (same Wi-Fi), not localhost.
# CMD: ipconfig | findstr IPv4
EXPO_PUBLIC_API_URL=http://192.168.1.44:8000

# Stops Expo from showing exp://127.0.0.1:8081 on Windows
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.44
